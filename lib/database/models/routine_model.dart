class RoutineModel {
  final int id;
  final String title, description, image;
  RoutineModel({
    required this.id,
    required this.title,
    required this.description,
    required this.image,
  });
}
