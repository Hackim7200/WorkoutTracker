class SetModel {
  // Set table and column
  final int setNumber, id, reps;
  final double weight;

  SetModel({
    required this.setNumber,
    required this.id,
    required this.reps,
    required this.weight,
  });
}
